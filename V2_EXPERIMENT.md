# V2 Design Experiment

Отдельная экспериментальная версия проекта на ветке `codex/v2-design-experiment`.

## Принцип

Сохраняем ядро продукта:

- `localStorage` и ключи данных;
- PWA-манифест;
- Service Worker;
- экспорт CSV / JSON / Markdown;
- существующие страницы и маршруты;
- privacy-first модель без сервера.

Меняем ощущение приложения:

- визуальная система стала ближе к рабочей станции практики, а не к дневнику-анкете;
- на десктопе вкладки вынесены в левую навигационную рейку;
- первый экран стал ежедневным пультом: состояние, КП, практика, след;
- добавлен быстрый выбор состояния перед записью;
- дневной показатель КП встроен в более широкий сценарий смены;
- формы стали плотнее, спокойнее и удобнее для регулярного ввода;
- лендинг получил тот же характер, что и v2-приложение.

## Сравнение

Запустить текущую основную версию:

```bash
cd "/Users/ebn/Cursor_Projects: andbox:/Дыхание /dyshinosom/docs"
python3 -m http.server 8080
```

Запустить эксперимент:

```bash
cd "/Users/ebn/Cursor_Projects: andbox:/Дыхание /dyshinosom-v2-experiment/docs"
python3 -m http.server 8081
```

Открыть:

- v1: `http://localhost:8080/app.html`
- v2: `http://localhost:8081/app.html`

## Что смотреть

- первый экран дневника;
- ощущение ежедневного пульта вместо анкеты;
- удобство ввода записи;
- ощущение навигации на десктопе и телефоне;
- читаемость журнала;
- тёмную тему;
- лендинг `index.html`.
